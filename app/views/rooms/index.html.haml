- content_for :title, "Rooms"

= render "header"

.body-grid
  = render "rail"
  = turbo_frame_tag "editor-panel", src: (@rooms.any? ? edit_room_path(@rooms.first) : nil) do
    %main
      .editor-wrap
        %p{style: "color: var(--text-faint)"} Loading…
