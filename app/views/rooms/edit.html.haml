- content_for :title, "Editing #{@room.name}"

= render "header"

.body-grid
  = render "rail"
  = turbo_frame_tag "editor-panel" do
    %main
      .editor-wrap
        - if notice.present?
          %p{style: "color: green"}= notice
        = render "form", room: @room
